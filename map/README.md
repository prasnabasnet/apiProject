# MAP

```
    Create Virtual Environment:
    1. python -m venv .env
    2. npm install
    
    For css open new terminal and type:
    1. cd src/lib/styles
    2. sass --watch style.scss:./style.css
```